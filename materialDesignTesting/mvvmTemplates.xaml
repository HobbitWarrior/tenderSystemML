<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:materialDesignTesting">
    
    <DataTemplate DataType="{x:Type local:LoginViewModel}">
        <local:LoginUC/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:UserViewModel}">
        <local:UserUS/>
    </DataTemplate>
    
    <DataTemplate DataType="{x:Type local:MainMenuWindowViewModel}">
        <local:MainMenuWIndow/>
    </DataTemplate>
    
    <DataTemplate DataType="{x:Type local:UserLoadStrategyViewModel}">
        <local:UserLoadStrategyView/>
    </DataTemplate>
    
    <DataTemplate DataType="{x:Type local:UserManualStrategyViewModel}">
        <local:UserManualStrategyView/>
    </DataTemplate>
    
    <DataTemplate DataType="{x:Type local:OpponentLoadStrategyViewModel}">
        <local:OpponentLoadStrategyView/>
    </DataTemplate>
    
    <DataTemplate DataType="{x:Type local:OpponentManualStrategyViewModel}">
        <local:OpponentManualStrategyView/>
    </DataTemplate>
    
    <DataTemplate DataType="{x:Type local:EstimateStrategyViewModel}">
        <local:EstimateStrategyView/>
    </DataTemplate>
    
</ResourceDictionary>